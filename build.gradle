allprojects {
    apply plugin: 'idea'
    apply plugin: 'maven'
}

subprojects {
    group = 'com.exmertec.elvis.scal'
    version = '0.0.1'

    repositories {
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.4'
}

